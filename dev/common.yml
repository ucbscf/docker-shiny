version: '2'

services:
  shiny:
    build: 
      context: .
      args:
        - SITE_SHINY_USER_ID
    volumes:
      - /mirror/data/pub/users/alucas/s20-2017f:/srv/shiny-apps
    #restart: always
    #logging:
    #  driver: syslog
    #  options:
    #    syslog-facility: "local7"
